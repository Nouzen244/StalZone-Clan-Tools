# 🎮 StalZone Clan Bot

> Бот для клана в игре **StalZone** (бывший STALCRAFT).

Discord-бот для управления кланом с **динамическим управлением** ролями, расписанием и детальным трекингом.

## ✨ Новые возможности v2.0

### 🔧 Динамическое управление
- **Роли через команды** — админы назначают роли без редактирования конфига
- **Выбор VC через команду** — `!setvc #канал`
- **Расписание отслеживания** — бот следит только в указанное время

### 📊 Детальный трекинг
- Время входа в ГС
- Время выхода из ГС
- Продолжительность каждой сессии
- Количество входов за день
- Связь с расписанием (сессией)

## 🚀 Быстрый старт

### 1. Установка
```bash
pip install -r requirements.txt
```

### 2. Конфигурация
Скопируйте `config.example.json` в `config.json` и впишите токен. Ключи `WEB_*`
управляют встроенным сайтом:
```json
{
    "TOKEN": "YOUR_BOT_TOKEN_HERE",
    "PREFIX": "!",
    "TIMEZONE": "Europe/Moscow",

    "WEB_ENABLED": true,
    "WEB_HOST": "0.0.0.0",
    "WEB_PORT": 8080,
    "WEB_PUBLIC_URL": "",
    "GUILD_ID": ""
}
```
- `WEB_PUBLIC_URL` — публичный адрес сайта (например, https-ссылка от Ngrok).
  Если пусто — ссылки `!site` ведут на `http://localhost:WEB_PORT`.
- `GUILD_ID` — какая гильдия считается «кланом» сайта (если бот в нескольких).
  Пусто — берётся первая.

### 3. Запуск
```bash
python main.py
```
Бот и **сайт поднимаются вместе** одним процессом. Сайт доступен на
`http://localhost:8080` (и на `WEB_PUBLIC_URL`, если задан).

### 4. Настройка на сервере
```
!setup              — мастер настройки
!setvc #канал       — установить VC для отслеживания
!setrole leader @Глава
!setrole officer @Офицер  
!setrole member @Клан
!schedule          — посмотреть расписание КВ (фиксированное)
```

## 🌐 Сайт клана (встроенный)

Сайт поднимается **вместе с ботом** (один процесс, общая база `data/bot_database.db`).
Бот — **источник истины**: участники, звания и посещаемость КВ синхронизированы в обе
стороны. Бот выдаёт краткую сводку, сайт хранит то же подробнее и позволяет править
вручную (офицер и выше).

### Вход на сайт
Логинов и паролей нет — вход только через Discord:
1. Напишите боту `!site` — он пришлёт в ЛС одноразовую ссылку и код (живут 10 минут).
2. Перейдите по ссылке (или откройте сайт и введите код).
3. Права на сайте = ваше звание: **Лидер/Полковник/Офицер** могут редактировать,
   остальные — только просмотр.

### Доступ из интернета (Ngrok)
Сайт слушает `0.0.0.0:WEB_PORT`. Для доступа извне и HTTPS используйте туннель:
```bash
ngrok http 8080
```
Полученный https-URL впишите в `WEB_PUBLIC_URL` (чтобы ссылки `!site` вели на него)
и перезапустите бота. Без HTTPS-туннеля коды/сессии шли бы по сети в открытом виде —
поэтому для интернет-доступа Ngrok обязателен.

### Что синхронизируется
| Данные | Источник | Где правится |
|--------|----------|--------------|
| Участники, звания | Discord/бот | бот (`!setrank`/роли) и сайт (офицер+) |
| Посещаемость КВ | трекинг бота | бот (`!kvedit`) и сайт (таблица «Активность») |
| Вики, галерея, тактдоска, заметки, календарь | сайт | сайт (хранятся на сервере) |

## 📋 Команды

### ⚙️ Настройка (Администратор)
| Команда | Описание |
|---------|----------|
| `!setup` | Мастер начальной настройки |
| `!setvc #канал` | Установить VC для отслеживания |
| `!setchannel report #канал` | Канал для отчётов |
| `!setchannel log #канал` | Канал для логов |

### 🪖 Звания (Администратор)
Иерархия фиксированная: **Лидер › Полковник › Офицер › Сержант › Боец › Рядовой**.
Звание участника = ручное назначение в боте → роль Discord → Рядовой.

Для каждого звания — своя команда (привязка роли Discord):

| Команда | Звание |
|---------|--------|
| `!setleader @роль` | 👑 Лидер |
| `!setcolonel @роль` | 🎖️ Полковник |
| `!setofficer @роль` | ⚔️ Офицер |
| `!setsergeant @роль` | 🛡️ Сержант |
| `!setfighter @роль` | 🔫 Боец |
| `!setprivate @роль` | 🪖 Рядовой |

Прочие команды:

| Команда | Описание |
|---------|----------|
| `!setrole <звание> @роль` | То же одной универсальной командой |
| `!removerole <звание> @роль` | Убрать привязку роли |
| `!setrank @игрок <звание>` | Назначить звание вручную |
| `!delrank @игрок` | Убрать ручное звание |
| `!rank [@игрок]` | Показать звание |
| `!roles` | Иерархия и привязки |

### 🕐 Расписание КВ (фиксированное)
| Команда | Описание |
|---------|----------|
| `!schedule` | Показать расписание КВ (изменить нельзя) |

Расписание зашито в коде и **не редактируется** командами.

### 📊 Посещаемость (Офицер+)
| Команда | Описание |
|---------|----------|
| `!report` | Отчёт о присутствии |
| `!stats @user` | Статистика игрока |
| `!top10` | Топ-10 активных |
| `!online` | Кто сейчас в VC |
| `!sessions @user [дата]` | Детальные сессии |
| `!export [дата]` | Экспорт в Excel |

### ℹ️ Информация
| Команда | Описание |
|---------|----------|
| `!status` | Статус отслеживания |
| `!ping` | Проверка бота |
| `!uptime` | Время работы |
| `!help` | Справка |

## 🕐 Расписание КВ (фиксированное)

Расписание постоянное и **не редактируется** командами. Все бои идут **20:00–21:00**:

| Дни | Событие |
|-----|---------|
| Пн, Вт, Ср | ⚔️ Потасовка |
| Чт, Пт, Сб | 🏆 Турнир |
| Вс | Потасовка / Захват базы (выбор) |

В **воскресенье** офицер выбирает событие — **Потасовка** или **Захват базы** — кнопками в отчёте `!kv`. Засчитывается одно событие.

Посмотреть расписание: `!schedule`.

## 🪖 Иерархия званий (фиксированная)

```
👑 Лидер
🎖️ Полковник
⚔️ Офицер
🛡️ Сержант      ← Офицер и выше = права управления
🔫 Боец
🪖 Рядовой
```

Другой иерархии быть не может. Звание берётся из ручного назначения
(`!setrank`) либо из привязанной роли Discord (`!setrole`), иначе — Рядовой.

## 📊 Что отслеживается

При каждом входе/выходе в VC записывается:

| Поле | Описание |
|------|----------|
| `join_time` | Точное время входа |
| `leave_time` | Точное время выхода |
| `duration_seconds` | Длительность в секундах |
| `date` | Дата (YYYY-MM-DD) |
| `user_id` | Discord ID |
| `display_name` | Ник в Discord |
| `channel_name` | Название канала |
| `schedule_id` | ID сессии (если по расписанию) |
| `status` | active / completed / interrupted |

## 📁 Структура проекта

```
clanbot/
├── main.py              # Главный файл
├── config.json          # Базовая конфигурация
├── requirements.txt     # Зависимости
├── bot.log             # Логи
├── cogs/
│   ├── __init__.py
│   ├── admin.py        # Управление, настройки
│   ├── attendance.py   # Посещаемость, отчёты
│   └── stalcraft.py    # Игровые функции
└── data/
    ├── bot_database.db # SQLite база
    └── clan_attendance.xlsx
```

## 🗃️ Структура БД

### guild_settings
Настройки каждого сервера (VC, каналы).

### guild_roles  
Привязки ролей Discord к званиям (leader, colonel, officer, sergeant, fighter, private).

### tracking_schedule
Расписание отслеживания (время, дни недели).

### voice_sessions
Детальные сессии (вход, выход, длительность).

### attendance
Суммарная посещаемость за день.

## 🔧 Права бота

В Discord Developer Portal включите:
- ✅ SERVER MEMBERS INTENT
- ✅ MESSAGE CONTENT INTENT

## 📝 Пример использования

```
# 1. Первичная настройка
!setvc #голосовой-канал
!setrole leader @Глава-клана
!setrole officer @Офицер
!setrole member @Клановец

# 2. Расписание КВ — фиксированное (только просмотр)
!schedule

# 3. Проверка статуса
!status

# 4. Отчёт по текущему КВ
!kv

# 5. Отчёт за конкретную дату
!kv 15-01-2026

# 6. Экспорт в Excel
!export
```

## ⚠️ Важно

1. **Intents** — обязательно включите в Developer Portal
2. **Права** — бот должен видеть каналы и роли
3. **Timezone** — укажите вашу временную зону в config.json

---

**StalZone Clan Bot** 🎮☢️
